# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios_client' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios_client
  pod 'Alamofire', '~> 4.7'
  pod 'Locksmith'
  pod 'Toast-Swift', '~> 3.0.1'
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'Stripe'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Cosmos', '~> 16.0'

  target 'ios_clientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios_clientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
